# name: etl-rs

# on:
#   push:
#     paths:
#       - 'etl-rs/**'
#       - '.github/workflows/rust.yml'
#   pull_request:
#     paths:
#       - 'etl-rs/**'
#       - '.github/workflows/rust.yml'

# defaults:
#   run:
#     working-directory: etl-rs

# jobs:
#   clippy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: dtolnay/rust-toolchain@stable
#         with:
#           components: clippy
#       - uses: actions/cache@v4
#         with:
#           path: |
#             ~/.cargo/registry
#             ~/.cargo/git
#             etl-rs/target
#           key: ${{ runner.os }}-cargo-clippy-${{ hashFiles('etl-rs/Cargo.lock') }}
#       - run: cargo clippy --workspace --all-targets -- -D warnings

#   test-unit:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: dtolnay/rust-toolchain@stable
#       - uses: actions/cache@v4
#         with:
#           path: |
#             ~/.cargo/registry
#             ~/.cargo/git
#             etl-rs/target
#           key: ${{ runner.os }}-cargo-test-unit-${{ hashFiles('etl-rs/Cargo.lock') }}
#       - run: cargo test --workspace --lib --bins

#   test-integration:
#     runs-on: ubuntu-latest
#     services:
#       redis:
#         image: redis:7-alpine
#         ports:
#           - 6379:6379
#         options: >-
#           --health-cmd "redis-cli ping"
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5
#       clickhouse:
#         image: clickhouse/clickhouse-server:24.8-alpine
#         ports:
#           - 8123:8123
#         env:
#           CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT: 1
#           CLICKHOUSE_PASSWORD: clickhouse123
#         options: >-
#           --health-cmd "wget --no-verbose --tries=1 --spider http://localhost:8123/ping"
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 10
#           --ulimit nofile=262144:262144
#     env:
#       E2E_REDIS_URL: redis://localhost:6379
#       E2E_CLICKHOUSE_URL: http://localhost:8123
#       CLICKHOUSE_USER: default
#       CLICKHOUSE_PASSWORD: clickhouse123
#     steps:
#       - uses: actions/checkout@v4
#       - uses: dtolnay/rust-toolchain@stable
#       - uses: actions/cache@v4
#         with:
#           path: |
#             ~/.cargo/registry
#             ~/.cargo/git
#             etl-rs/target
#           key: ${{ runner.os }}-cargo-test-integration-${{ hashFiles('etl-rs/Cargo.lock') }}
#       - run: cargo test --workspace --tests
