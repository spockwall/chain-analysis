# Root compose file. Sub-stacks live under compose/ and are merged via `include`.
# Requires Docker Compose v2.20+.
include:
  - compose/infra.yml
  - compose/app.yml
  - compose/etl.yml
  - compose/dagster.yml
